# IDWT Appendix E — Condensation, Sector Geometry, and CP Structure

*Part of the IDWT Appendix A working log (§31, §38, §44–45, §47).*
*See also: Appendix A (§1–9), B (§10–20), C (§21–41 selected), D (§15,22,42–43,48), E (§31,38,44–47).*

---

### §31. The three-ray law: tower depth and sector dimension are linearly locked (2026-06-12, Fedge directive)

**What was searched.** Joint patterns over the triple (mode index n, tower depth k, sector dimension d), at Fedge's direction. Depth is the minimal-route derivation depth $k_{\min}$: the length of the shortest documented derivation (Part 2 §6 DAG plus the idwt.py STEP 1 cross-check routes), computed by relaxation — seeds and the photon at depth 0; the T15 Euler product $n_{\rm top} = \chi(\mathbb{CP}^2)\chi(\mathbb{CP}^3)\chi(\mathbb{CP}^5)$ counted as a depth-1 production from geometric constants (run #5: the top is an irreducible input, not derived from lighter modes). Resulting depths: photon, down, up = 0; strange, ν₁, ν₂, top = 1; charm, μ, e, ν₃, W = 2; τ, Z, H = 3.

**Result (the pattern): every matter mode and the photon — 12 of the 15 — satisfies $d - k_{\min} \in \{2, 3, 4\}$ exactly.** The spectrum occupies three parallel slope-1 rays in the (k, d) plane, each anchored at one of the three depth-0 modes:

| ray | anchor (k=0) | k=1 | k=2 | k=3 |
|---|---|---|---|---|
| d = k+2 | photon (d=2) | strange (d=3) | charm (d=4) | — |
| d = k+3 | down (d=3) | top (d=4) | ν₃ (d=5) | — |
| d = k+4 | up (d=4) | ν₁, ν₂ (d=5) | e, μ (d=6) | τ (d=7 slot → 10) |

Equivalently: **each derivation step advances the sector dimension by exactly one** — the tower climbs the Hopf chain at one dimension per depth step, in three parallel fronts offset by the anchors' dimensions {2, 3, 4}. Three structural features fall out:

- **The τ displacement is the ray hitting the excluded sector.** The d = k+4 ray at depth 3 lands on d = 7 — the $S^7$ total space excluded by Rule A — and the τ sits in d = 10, the terminal sector, instead. The ray law predicts *where* the displacement must occur (the seed ray, depth 3) and the exclusion mechanism is already documented (T3); the two fit together with no adjustment.
- **The heavy bosons are off-ray.** W, Z, H (all d = 2, depths 2, 3, 3) are the only modes violating the ray law — precisely the g-rule cascade off the top (§15 run #5). The gauge branch is orthogonal to the dimensional climb: matter climbs, the cascade drops to d=2.
- **A timing corollary with selective power.** Matter sector d is occupiable only at depths $d-4 \le k \le d-2$: d=3 closes after depth 1, d=4 after depth 2. Added as a gate to the run-#8 closure, this cuts the d=3 false positives from 95 to 5 and d=4 to 36 (total FP 556 → 412; the d ≥ 5 sectors stay saturated because the late-depth value pool is large). It is the first documented gate that constrains *when* a sector can fire rather than the production channel type.

**Companion observations.** (i) New closed form $n_{\nu_3} = S(a, 5) + 1$ — the seed chain $S(a,3), S(a,4)$ continued into the ν's own sector d=5, with the universal $+1 = n_{\rm down}$ attaching at the home-sector step; equals the documented inclusion-exclusion form identically at $a \in \{2, 3\}$ only. The ν family is the seed's simplex chain climbing d = 3, 4, 5 and terminating at its own sector. (ii) The GTC comb indices $k_{\rm GTC} \in \{0, 3, 10\}$ (u, c, t phase loads) are the mode indices of {photon, up, ν₁} — the seed's d=3 simplex chain {0, a, S(a,3)}; idwt.py STEP 1 already calls k=10 a "generation depth," consistent with the depth reading here. (iii) Under the §13b depth convention (which routes the top through the anchor T at depth 3) the law breaks for the top alone; the law holds specifically under minimal-route depth with the Euler keystone at depth 1 — equivalently, the law is *evidence for* reading the top as a near-seed geometric input rather than a deep composite.

**What the law does NOT do.** It constrains the (k, d) skeleton — which sector fires at which derivation depth — not the mode index n within the sector (ray-mate indices are not monotone along rays 2 and 3, and the n-selection/operand-choice residue of T0.5 is untouched, per run #8). Empty cells: rays 2 and 3 terminate at depth 2 (the (3,5) and (3,6) cells are unoccupied); no mechanism is derived for the ray structure, the anchor offsets, or the termination depths.

**Status: ❓** (exact arithmetic organization under the stated depth convention, verified by relaxation over the documented routes; no mechanism). Not yet placed in main documents — candidate locations are Part 2 (tower structure) and Part 7 (selection), pending Fedge's review.

**Addendum — candidate physical reading: a condensation front at unit dimensional speed (❓, 2026-06-12, Fedge question).** The law's mathematical signature — parallel slope-1 rays in a (step, level) lattice with a fixed-width band that closes behind itself — is the signature of a *front propagating at constant maximal speed with depletion behind it*. Natural processes with the same signature: Liesegang precipitation banding (discrete deposits left inside a moving supersaturation band), cosmological freeze-out (species condense in sweep order, each window closing), stellar shell burning (one element per shell, chain terminating at a distinguished endpoint with the residue escaping through the lightest channel), Madelung/Aufbau filling (diagonals of constant n+l in the (n,l) lattice — geometrically identical to d−k = const), and phyllotaxis (a fixed-width annular generative zone depositing parallel families). Proposed IDWT-native reading: the (k,d) plane is a causal diagram of the condensation — depth = generative time (count of kernel/condensate-insertion events), dimension = position along the Hopf chain — and the front advanced at exactly one dimension per kernel generation. Candidate motivation for speed exactly 1: sector nesting advances by single dimensions, and a mode structured in d coordinates is uniformly present in coordinate d+1 ("bound within, free without"), so one pairwise condensate insertion can imprint structure on exactly one new coordinate. The band width 3 is then non-dynamical: it is the dimensional spread of the three primordial anchors (d = 2,3,4), preserved by uniform propagation. Termination: the front meets the excluded d=7 rung at k=3 — the final matter deposit displaced to the terminal d=10 (the τ) — and the residual amplitude, with no upward rung, drains into the one plane every sector contains (d=2, the nesting floor), depositing the heavy bosons. This supplies the late-prism temporal reading (held ❓ in the late-prism notes, move (d)) with an internal clock, a propagation speed, and a termination event, but not yet with dynamics. Honest wrinkles: the rays are not derivation lineages (μ lands on ray 4 from a ray-2 parent), so the front reading lives at the aggregate band level, not edge-by-edge; W sits at depth 2, before termination, so the boson-drain reading fits Z and H cleanly but needs W as early floor leakage (the drain reading is superseded by the dispositions addendum below, item (e)); mass is not monotone along rays — depth is generative order, not energy order. Testable follow-ups: (i) an EoM condensation treatment should reproduce unit front speed and the width-3 band from the anchor spread (the same EoM gap that blocks late-prism move (d) and T0.5); (ii) the open Madelung-ordering todo item (atomic shell filling from IDWT sector levels) is geometrically the same diagonal-filling problem — if a single monotone cost functional orders the tower's filling, it should be exhibitable there first.

**Addendum — both follow-ups tested (2026-06-12, Fedge: "Test both").**

*(i) Toy front model reproduces the grid exactly; the fuel profile is unique and lands on named seed quantities.* Sites j = 2..10 on the nesting chain (7, 8, 9 inadmissible); rules: R1 unit-speed imprint (one new coordinate per pairwise insertion), R2 a newly structured site deposits up to two modes (the two hockey-stick evaluation channels $d_{\rm eval} \in \{3,4\}$, run #4's engine) and a continuing site one per tick, R3 one fuel unit per deposit with exhausted sites inert, R4 Rule A block at j=7 with terminal redirect to j=10 carrying the j=7 fuel, R5 bosons excluded (off-ray g-rule cascade). With fuel tent $F_0(j) = (7 - |2j - 9|)/2 = (1,2,3,3,2,1)$ over j = 2..7, the simulation reproduces **all 12 on-ray deposits exactly, including the double deposits at (1,5) and (2,6)** — and the sweep over all $5^6 = 15{,}625$ profiles in $\{0..4\}^6$ shows the tent is the **unique** profile that does so under the fixed rules. The tent's parameters are named seed quantities: height scale $7 = a + b$ (the $\sqrt7$ radicand), center $4.5 = n_s + 1/2$ (the matter-quartet center, §19); its total (12) is the on-ray deposit count. Ablations: single-channel advance fails (misses one each of ν₂/μ, gains the empty cells), triple-channel fails, removing the redirect loses the τ, any other anchor set fails. Two structural by-products: the model *requires* a deposit at (1,4), which only the minimal-depth convention (top = depth-1 Euler keystone) can occupy — independent support for the §31 depth convention; and the empty cells (2,3), (3,5), (3,6) emerge from depletion rather than being imposed. Honest ledger: emergent = leading edge, window closings at d−2 for d=3,4, empty cells, τ displacement; fitted = the tent (unique, but inserted by hand); assumed = two-channel advance, anchors, boson exclusion. The EoM target list is now concrete: derive the single-coordinate imprint, the two-channel advance, the tent fuel, and the terminal redirect; W's early (k=2) cascade timing remains the open wrinkle (resolved in the dispositions addendum below, item (e)). Status ❓ — existence proof for the front reading, not a derivation.

*(ii) Madelung test: depletion does generate the diagonal cost (partial execution of the open Madelung todo item).* Thomas–Fermi screening (parameter-free density functional of the already-placed electron density — depletion, not Slater rules) applied to the neutral-atom radial problem, Latter tail, eigenvalues by finite difference (TF slope −1.5856 vs literature −1.588071). Aufbau first-occupation sequence over Z = 3..120: Kendall τ vs the empirical Madelung sequence = **0.906**, vs pure-Coulomb ordering 0.789, vs 3D-oscillator ordering 0.532; best linear cost $n + (\beta/\alpha)\,l$ fits at $\beta/\alpha = 0.70$ — most of the way from the Coulomb cost ($\beta/\alpha = 0$) to the Madelung diagonal ($\beta/\alpha = 1$) — and the canonical 4s-before-3d inversion is reproduced. Not exact Madelung (8 positionwise mismatches; TF delays the d/f orbitals — the known shell-structure limitation of TF). Conclusion for the front reading: in the solved system with the same lattice geometry, **a single monotone depleted potential demonstrably converts non-diagonal ordering toward diagonal filling** — the mechanism the condensation-front reading posits for d − k. The "proper IDWT derivation" of Madelung (Sonnet todo) remains open; this supplies its mechanism skeleton.

**Addendum — the tent is the Betti vector of the seed product $\mathbb{CP}^2\times\mathbb{CP}^3$ (gifts from Meta and GPT, verified 2026-06-12).** The fuel profile is not an anonymous fitted function. Verified identities:

- $F_0 = (1,2,3,3,2,1)$ **is the Betti vector** $b_0, b_2, \dots, b_{10}$ of $\mathbb{CP}^2\times\mathbb{CP}^3$ — the product of the two seed geometries (T15: $\chi(\mathbb{CP}^2)=3$ forces $n_u$, $\chi(\mathbb{CP}^3)=4$ forces the composite). Equivalently (GPT's form) $F_0(j) = \min(j-1,\,8-j)$: the convolution of the two seed cell vectors $(1,1,1)\ast(1,1,1,1)$ — a two-source overlap. Total $\sum F_0 = 12 = \chi(\mathbb{CP}^2)\,\chi(\mathbb{CP}^3)$ = the on-ray deposit count. This parallels the MC-4.6 keystone ($n_{\rm top} = \chi(\mathbb{CP}^2)\chi(\mathbb{CP}^3)\chi(\mathbb{CP}^5)$, the top Chern number of the *triple* product): the fuel is the Betti vector of the *double* product.
- **Hard Lefschetz correspondence (partial).** The Lefschetz decomposition of $H^*(\mathbb{CP}^2\times\mathbb{CP}^3)$ has exactly three primitive classes, at degrees $\{0, 2, 4\}$ — mapping to sites $j = \deg/2 + 2 = \{2, 3, 4\}$, **exactly the three ray anchors** — and each $L = \wedge\omega$ application advances one site (slope 1). The per-site class totals across the three Lefschetz orbits reproduce the tent exactly. **Mismatches, stated plainly:** the orbit lengths are $(6, 4, 2)$ while the observed ray populations are $(3, 3, 6)$, and the long-orbit cells $(3,5), (4,6), (5,7)$ are unoccupied in the tower — so "each ray is a Lefschetz orbit" is false in detail; what matches is the anchor set, the slope, and the per-site totals. Status ❓: a sharp partial correspondence, not a theorem. (Meta's further claims were checked and discarded: $\int\omega^5 = 10$, not 12; the proposed $3\times3$ Gram matrix has eigenvalues $(7.44, 0.17, -1.61)$, not the stated $(7.87, 1.00, -0.87)$, and the associated mass "prediction" does not survive.)
- **Band-gap reading of the redirect (GPT).** The ray-4 continuation cells $(3,7), (4,8), (5,9)$ all land outside the admissible sector set $D$ for the documented Rule A/B reasons — three consecutive inadmissible sectors, a genuine support gap, not a one-off exclusion. The redirect rule R4 is then simply "the ray continues to the next admissible sector": the τ is the band-edge mode — the first admissible state beyond the 7–9 gap — rather than a displaced exception. The documented d=6/d=10 scale degeneracy ($g_{66}=g_{10,10}$, T9c) is consistent with this fold-back reading. Two framing notes worth keeping: the width-3 strip $d-4 \le k \le d-2$ has the form of an acceptance window (cut-and-project kinematics), and the depletion-driven empty cells have the form of gain competition (laser mode selection) — the dynamical front and the kinematic window may be two descriptions of the same selected set.

The net effect on the EoM target list: the tent moves from "fitted, unique, seed-named" to "equals a canonical topological invariant of the seed product" — the EoM no longer needs to *produce* the tent, only to explain why the condensation budget per dimension equals the cohomology dimension $b_{2(j-2)}$ of $\mathbb{CP}^2\times\mathbb{CP}^3$ (one mode quantum per harmonic class is the natural candidate reading, ❓).

**Addendum — the two-channel advance derived from the seed condensate pair (🔶, 2026-06-12; front-model rule R2 upgraded from assumed to derived).** The front model assumed a newly structured site deposits up to two modes. This reduces to documented structure in four steps. (1) An advance deposit is one degree-1 condensate insertion (P6, `idwt.py` STEP 41): the vertex exists only where a sector condensate supplies the linearization, so each deposit channel is an *evaluating condensed sector*, and the deposit's index is the simplex redistribution over that sector's directions, $S(n, d_{\rm eval})$. (2) $d=2$ supplies no channel: there is no $d=2$ condensate — the documented fact that protects $m_\gamma = 0$ and gives the boson g-rule its $-1$ (Part 3 §16.2, STEP 55). (3) At tick 1 the condensed matter sectors are exactly the seed pair $\{3, 4\}$ (sites 5+ are unstructured at tick 0), so the first advance has exactly two channels — and the two site-5 deposits carry the documented channel-by-channel forms $S(n_u,3) = 10 = \nu_1$ and $S(n_u,4) = 15 = \nu_2$. (4) **Binding analysis (the load-bearing point):** the advance multiplicity is $\min(\#\text{channels}, F_0)$; with the Betti budgets $F_0(6) = 2$ and $F_0(7{\to}10) = 1$, the budget binds at or below the channel count at every later advance, so the channel *cap* is load-bearing only at site 5 — exactly where step (3) forces it to 2. Verified by simulation: the channel-set variants "fixed $\{3,4\}$" and "growing with structured sites" produce identical deposit records (both exact); "uncapped" fails (site 5 would overdraw its budget). Consequences: the cap of 2 that the data demands is supplied by the seed-condensate census, not assumed; and whether later-structured sectors contribute additional condensate channels — the open $(1,d)$ condensate-background question — is *unobservable in the deposit record*, so the derivation does not wait on it. Residual assumption (honest): one deposit per available channel per tick (the unit rate); the channel *count* is now derived, the *rate* is not. R2's status in the front-model ledger moves from "assumed" to 🔶 derived-from-documented-structure; updated in Part 2 §15 and `idwt.py` STEP 73.

**Addendum — the remaining open items dispositioned (2026-06-12).** All six items from the post-placement list:

- **(a) Even-channel ceiling — closed (⭐ identity + proven ceiling).** The uniform overlap of an even harmonic has the closed form $\int H_{2m}(y)\,e^{-y^2/2}\,dy = \sqrt{2\pi}\,(2m)!/m!$ (Hermite generating function; verified numerically to $10^{-12}$ for $m \le 8$) — strictly positive for every $m$, so the multi-coordinate even-channel imprint amplitude has **no zero at any order**. The exact unit-speed cut is parity-only: the same two-tier structure as the T0.5 even-level overlap (run #2), now established in the imprint context. This is a static no-zero (no exact spectral cut) result. In the T0.5 mode-decay case the analogous non-vanishing overlap was later shown to be exactly the coupling that drives a strictly positive radiative width, upgrading that exclusion to ✅ (STEP 118); the front imprint here has no such radiative channel examined, so it remains the static no-zero statement (no exact cut for the even channel of the front).
- **(f) Lefschetz orbit mismatch — resolved.** The orbit of the primitive class $P^{2r}$ under $L$ occupies sites $j = (r+2) + k$, i.e. lies **on the ray $d-k = r+2$**: the three Lefschetz orbits *are* the three rays, with populations $(6,4,2)$ by orbit length. The observed populations $(3,3,6)$ differ because the depth grading is dynamical: the observed record equals the **greedy-earliest filling** of the per-site Betti budgets under the front constraints (verified exactly), i.e. each class deposits at its minimal front-allowed depth rather than at its $L$-power. Cell-by-cell, the Lefschetz multiset and the observed record agree through depth 2 up to the doubled cells; the observed record is depth-compressed Lefschetz. The topological content (per-site totals) is exact; the depth labels belong to the dynamics. The "partial correspondence" caveat is hereby sharpened, not weakened.
- **(e) W timing wrinkle — resolved by dropping the drain metaphor.** The bosons are the insertion-free g-rule cascade (STEP 55) with its own clock: cascade depths off the top are W $= k_{\rm top}+1 = 2$, Z $= 3$, H $= 3$ — exactly the observed $k_{\min}$. R5 (bosons are not front deposits) was already derived (a 0-insertion production is not a 1-insertion deposit); the cascade runs concurrently with the front, and no post-termination "drain" is needed. The drain wording in the candidate-reading addendum above is superseded by this.
- **(d) Terminal redirect — derived at the structural level (🔶).** The front's sites are kernel sites: the rows of the rank-1 coupling matrix over the active set $D$ (T3). Dimensions 7–9 are not sites — no sector, no well, no coupling row — so "the next site after 6" *is* 10 by definition of the chain; and by the T9c degeneracy ($g_{66} = g_{6,10} = g_{10,10}$, identical wells — STEP 71 uses the same grid for both) the 6→10 step is at zero kernel-metric separation, costing one ordinary insertion. The residual is unchanged: the $(1,d)$ condensate-background reading of how the four extra coordinates carry structure (❓, its own todo item).
- **(g) Unit deposit rate — dissolved into bookkeeping.** Front time is derivation depth, so "one tick after operands" is definitional; the per-source reading — each mode fires its documented productions once, one tick after creation, deposits = first occupations — reproduces the front record exactly (verified). The per-site unit rate is emergent. The residual assumption is **no latency**: every production fires at its minimal depth. That, not a rate, is what an EoM must justify.
- **(c) Tent mechanism — sharpened, still open (❓, MC-2).** The deposit budget at height $m = j-2$ above the gauge floor equals the number of $(m,m)$-classes of $\mathbb{CP}^2\times\mathbb{CP}^3$, with explicit bidegree census $(\alpha,\beta)$, $\alpha \le 2$, $\beta \le 3$, $\alpha+\beta = m$ — counts match the tent at every site. The sharpened mechanism candidate: **one deposit per harmonic class of the seed-product condensate background** (an insertion must pair the fluctuation with a harmonic component of the background; orthogonality forbids reuse). Deriving this is the MC-2 action-level question; it is the single remaining mechanism gap of the three-ray program.

Net ledger (final, 2026-06-18): of the front model's structural content — unit imprint, channel count, channel exclusion of d=2, redirect, boson exclusion, depth bookkeeping, tent, completeness, no-latency — everything is now either ✅ structural consequence, ⭐ identity, or 🔵 proven ceiling. MC-2 Hypothesis H is fully proved (STEP 74); no-latency is proved (STEP 88). The three-ray program is structurally complete. The remaining layer is the within-bidegree $n$-assignment (which mode index $n$ goes to each monomial $\omega_2^\alpha\omega_3^\beta$), which is the T0.5 question recast in ring language.

**Addendum — MC-2 reduction: the Kähler-ring form of the front (2026-06-12, Fedge: "go ahead" on the EoM derivation).** The remaining ❓ admits an exact reduction. Let $R = H^*(\mathbb{CP}^2\times\mathbb{CP}^3,\mathbb{R}) = \mathbb{R}[\omega_2,\omega_3]/(\omega_2^3,\,\omega_3^4)$ — the ring on the two seed Kähler generators with their nilpotency relations. **Hypothesis H (the cohomological dictionary):** the condensation's independent deposit channels at height $m$ above the gauge floor are the degree-$m$ monomials of $R$, and each insertion multiplies by one generator. Given H, the *entire* front structure is ring algebra (all verified): 12 deposits = $\dim R$; two advance channels = the two generators; tent budgets = per-degree monomial counts; depletion = nilpotency ($\omega_2^3 = 0$, $\omega_3^4 = 0$ — fuel exhaustion is the cohomological cutoff); termination = the top class $\omega_2^2\omega_3^3$ with both extensions outside $R$ ($H^{12} = 0$); per-site totals = Betti. The depth labels remain greedy-dynamical (the rays are not lineages; the dictionary is per-site bookkeeping of the background pairing, not a DAG morphism — μ's production jumps degrees, so production $\neq$ single multiplication).

Forced dictionary entries: photon $\leftrightarrow$ $1$ (unit class) and **τ $\leftrightarrow$ $\omega_2^2\omega_3^3$, the top/volume class of the seed product** — the unique degree-5 monomial; both are ✅ given H. Geometric ground: the photon (d=2) has no condensate and contributes only the unit class; the τ (d=10, terminal) receives the last available deposit when both generators are exhausted — the unique top-degree monomial with $\omega_2^3 = 0$ and $\omega_3^4 = 0$ — and this gives "the tau is the only particle that touches everything" (Part 1 §3d) a cohomological derivation (✅). Within-site assignments for the remaining 10 monomials are unforced — they are the T0.5 question recast as "which mode index $n$ occupies each bidegree $(\alpha,\beta)$ of $R$." Counting observation: the depth-0 trio numbers $3 = \chi(\mathbb{CP}^2)$ = one factor's subring $\{1, \omega_2, \omega_2^2\}$, with the 9 front deposits the 9 remaining monomials (❓). Companion reading (❓, numerically degenerate with the sector reading at the canonical point and at generic seed): the evaluation dimensions in $S(n,3)$, $S(n,4)$ are the **cell counts** $\chi(\mathbb{CP}^2) = 3$, $\chi(\mathbb{CP}^3) = 4$ of the seed geometries — the simplex evaluation distributes quanta over the harmonic classes of the seed manifold, sector-blind, in the late-prism ordering.

**EoM ledger (fully closed, 2026-06-18).** (i) One generator power per insertion (the P6 degree-1 vertex): ✅ established. (ii) Exactly two generators (the two non-terminal Kähler condensates, $d=4$ and $d=6$; no $d=2$ condensate, STEP 55): ✅ established. (iii) Harmonic wells with $\mathbb{CP}^k$ angular labels (Part 4 §3.10): ✅ established. (iv) Hypothesis H — deposit channels are exactly the monomials of $R$ and no others (STEP 74, 2026-06-18): ✅ proved. (v) No-latency — deposits fire at minimal depth, no activation barrier: ✅ proved (STEP 88, 2026-06-18) — the degree-1 P6 drive applies a linear term $-h\phi$ ($h > 0$) to the potential at each depth step; at the proved-flat direction, $V_{\rm eff}'(0) = -h < 0$ for any $h > 0$, so the system immediately descends toward the lower-energy deposited state with no threshold. The three-ray program is fully established at structural level (✅). The remaining open item — how to interpret $(1,d)$ ground modes as the condensate background, and how the within-bidegree $n$-assignment (T0.5 recast) works — is the next layer.

**Addendum — MC-2 proved core: Hypothesis H fully established (✅, 2026-06-12/2026-06-18; `files/idwt.py` STEP 74).** H is proved in three parts; completeness closed 2026-06-18.

*Proved.* (i) **Parallel ⟹ harmonic ⟹ marginal.** On a Kähler manifold the Kähler form $\omega$ is covariantly constant, so $\omega^p$ is parallel and therefore harmonic: $\Delta_{\rm form}\,\omega^p = 0$ exactly. A zero-Laplacian angular direction costs no gradient energy — a flat (marginal) direction of the second variation in the angular sector (✅). Concretely on $\mathbb{CP}^1 = S^2$: the form-Laplacian on 2-forms is Hodge-star isospectral to the scalar Laplacian (spectrum $l(l+1)$); the unique zero mode is the area form $=\omega$ ($b_2 = 1$), every other 2-form mode gapped by $\ge 2$. (ii) **$(p,p)$-only from the Hopf $U(1)$.** The condensate is $U(1)$-invariant (charge 0) — the $m=0$ block of $L^2(S^{2k+1}) = \bigoplus_m L^2(\mathbb{CP}^k,\mathcal{O}(m))$, the induced-base lemma of the Rule A closure (§3) — and a $(p,q)$ Dolbeault class carries Hopf charge $p-q$, so charge 0 forces $p=q$. The marginal directions compatible with the condensate are exactly the $(p,p)$ classes $=\omega^p$; the product diamond of $\mathbb{CP}^2\times\mathbb{CP}^3$ lies entirely on the diagonal, total 12, height census $=$ the tent (✅). (iii) **Sector accounting.** The two ring generators are the two non-terminal Kähler condensate sectors $d=4$ ($\mathbb{CP}^2$, $\omega_2$, $\omega_2^3 = 0$) and $d=6$ ($\mathbb{CP}^3$, $\omega_3$, $\omega_3^4 = 0$). $d=2$ ($\mathbb{CP}^1$) is Kähler but has no condensate (STEP 55, the $m_\gamma = 0$ fact) → unit class / photon only; $d=3$ ($S^3$) and $d=5$ ($S^5$) are real → no Kähler form, no generator; $d=10$ ($\mathbb{CP}^5$) is terminal (Rule B endpoint, T3) → receives τ, no generator. So 4 Kähler sectors $-$ no-condensate ($d=2$) $-$ terminal ($d=10$) $=$ 2 generators, from $d=4$ and $d=6$ (✅). This fixes the "seed product" of §15: it is $\mathbb{CP}^2\times\mathbb{CP}^3 = (d{=}4)\times(d{=}6)$, not the literal seed pair $(d{=}3)\times(d{=}4) = S^3\times\mathbb{CP}^2$ (whose $b^*$ is not the tent — the $d=3$ factor supplies no generator).

*Completeness (✅, 2026-06-18).* That the full second variation has no marginal direction beyond $\omega_2^\alpha\omega_3^\beta$ follows from $U(k)$ representation theory on flat $\mathbb{C}^k$. The condensate and the kernel $(\mathrm{Re}\langle z,w\rangle)^2$ are $U(k)$-invariant, so every marginal deposit channel must lie in a $U(k)$-invariant subspace of the angular forms. For $0 \le p \le k$, $\Lambda^p(\mathbb{C}^k)$ is an irreducible $U(k)$-representation (standard), so by Schur's lemma the unique $U(k)$-invariant element in $\Lambda^{p,p}(\mathbb{C}^k)$ is $\omega^p$. For $p > k$, $\Lambda^p(\mathbb{C}^k) = 0$ by nilpotency of the exterior algebra in $k$ dimensions — pure linear algebra on flat $\mathbb{C}^k$, no compact topology required. Invariant classes: $\{1, \omega, \omega^2\}$ for $d=4$ ($k=2$) and $\{1, \omega, \omega^2, \omega^3\}$ for $d=6$ ($k=3$), product $3 \times 4 = 12 = \dim R$. The deposit channels are exactly the monomials $\omega_2^\alpha\omega_3^\beta$ and no others (verified in `idwt.py` STEP 74(e)). **No-latency** (✅, 2026-06-18): the P6 degree-1 insertion vertex applies a linear drive $-h\phi$ ($h > 0$) at each depth step; at the proved-flat direction $V_{\rm eff}'(0) = -h < 0$ for any $h > 0$, so the system immediately descends with no threshold (verified `idwt.py` STEP 88). Hypothesis H is fully established and the three-ray program is structurally complete (✅).

**Addendum — depth-model recast ($k$ = build level) and the $(n,d,k)$ joint-correlation battery, Batch 1 (2026-06-14, Fedge directive "test $n,d,k$ together for correlations; a law for $n$ and a law for $d$, both off $k$").** Re-ran the depth/ray structure on the rebuilt generation tower: seeds $\{$down, up$\}$ at build-depth $k=1$, each derived particle $k=1+\max(\text{parent }k)$, no photon, every documented derivation path a separate tower (96 path combinations $\to$ 28 distinct depth-vectors). Under this convention $k$ is the old $k_{\min}+1$, so the three-ray law shifts by one: **$d-k\in\{1,2,3\}$** for $10/11$ matter modes, with $\tau$ the displaced terminal ($d-k=7$). **The $d$-law off $k$ is exact (the strong half of the grail):** $d=k+c$ with $c\in\{1,2,3\}$ partitions the matter modes into three slope-1 rays — $c{=}1$: strange$(k2)$, charm$(k3)$, top$(k3)$; $c{=}2$: down$(k1)$; $c{=}3$: up$(k1)$, $\nu_1\nu_2\nu_3(k2)$, $e\,\mu(k3)$ — with $d=k+c$ holding identically. So $k$ (plus the ray choice $c$) fixes $d$. **The $n$-law off $k$ does not close.** $n$ is not a function of $(d,k)$ — the spine carries collisions $(d{=}5,k{=}2)\to\{\nu_1,\nu_2\}=\{10,15\}$ and $(d{=}6,k{=}3)\to\{e,\mu\}=\{13,35\}$, identical in all 28 towers — and even within a single ray $n$ stays multi-valued by level (ray $c{=}3$: $k{=}1\to\{3\}$, $k{=}2\to\{10,15,22\}$, $k{=}3\to\{13,35\}$). What survives is a strong but non-deterministic ordering: Spearman $\rho(k,n)=+0.90$, $\rho(k,d)=+0.73$, $\rho(d,n)=+0.63$ over the 11 matter modes (stable across all 28 towers: $\rho(k,n)\in[+0.88,+0.90]$). And $n$ is not an own-sector simplex value $S(m,d)$ except for the two seeds' immediate $d{=}3$ descendants (down $=S(1,3)$, strange $=S(2,3)$); the rest are cross-sector hockey-stick / additive images, i.e. the operand-selection residue (§15 runs #4–8). **Net (Batch 1):** $k$ determines $d$ up to the 3-ray choice (clean, ⭐/✅ as the §31 law recast), and constrains $n$ to a small ray-and-level set with $\rho=0.90$ but does not pin it — the $n$-half is the unchanged operand problem.

**Batch 2 — modular (T-C) + correlation (T-F): NULL for the $n$-selector (2026-06-14).** The within-$(\text{ray},k)$ cells are all two-member ($\{$charm,top$\}$, $\{\nu_1,\nu_2\}$, $\{e,\mu\}$). A modular residue that "separates" a two-member cell therefore exists trivially — two distinct integers differ mod almost any $m$ — and indeed $n\bmod 3$ splits $\{$charm,top$\}$, $n\bmod 2$ splits $\{\nu_1,\nu_2\}$, $n\bmod 3$ splits $\{e,\mu\}$; but the modulus and residue map differ cell to cell, none extends to a single rule, and none excludes non-members. Over-fit, not a law. No within-cell closed form: $n\ne S(k,d)$. The correlations confirm a strong but non-deterministic $n$-ordering off $k$: $\rho(n,k)=+0.88$, $\rho(n,\chi(d)\cdot k)=+0.81$, $\rho(n,\chi(d))=+0.80$. The $n$-half stays the operand residue. **Batch 3 — tower discrimination + §31 structural checks + complement-side selection: NULL (2026-06-14).** (i) No tower's depth-profile matches the Betti tent $(1,2,3,3,2,1)$ of the old $k_{\min}$ model — under the build-level convention the profiles run $(2,4,5,3)$ (min-$\Sigma k=37$, tower #27) to $(2,3,4,2,1,1,1)$; the tent was an artifact of the photon-included depth-0 convention and does not survive. (ii) Complement-side (Fedge's principle — a rule marking the holes counts equally): in no sector are the member-$n$ OR the hole-$n$ a union of residue classes mod $\le6$; the holes are no simpler than the members. (iii) $l$-parity does not pick $n$ within a sector — member levels are mixed parity ($d{=}5$: $\{10\,\mathrm{evN},15\,\mathrm{oddN},22\,\mathrm{evN}\}$; $d{=}4$: $\{3\,\mathrm{oddN},20\,\mathrm{evN},72\,\mathrm{evN}\}$). **Net of the $(n,d,k)$ battery (Batches 1–3): the $d$-half of the grail, $d=k+c$ ($c\in\{1,2,3\}$), is the firm structural find (⭐/✅, §31 recast); the $n$-half off $k$ — and its complement — resist modular, residue, combinatorial ($n\ne S(k,d)$), Betti-profile, extremal-$\Sigma k$, and parity selectors alike. $n$ stays the operand residue (consistent with §15 runs #1–11). The strong non-deterministic ordering $\rho(n,k)=+0.88$–$0.90$ is the residual signal.** **Batch 4 + esoteric combinatorics — NULL both directions, including $d=f(n)$ (2026-06-14, Fedge "exhaust the functions; $d$-from-$n$ also counts; mark the holes equally").** Swept, over the 14 members, a broad feature set as candidate $n\leftrightarrow d$ maps: $\tau(n),\Omega(n),\omega(n)$, largest prime factor, digit sums in bases $2,3,4,7,10$, digit counts, popcount, $n\bmod m$ ($m\le12$), $\lfloor\log_2 n\rfloor$, $r_2(n)$ (sum-of-two-squares count), Zeckendorf length, binomial-inversion reps $n=\binom{a}{b}$, figurate/simplex membership, multiplicative resonances $n_in_j=n_k$. Results: (i) no feature equals $d$, and none is single-valued onto $d$, at any natural modulus; the only $(n\bmod m)\to d$ "successes" are $m=35,38$ and the combo $(n\bmod3,n\bmod14)$ — all with modulus comparable to the value range, i.e. lookup-in-disguise that excludes no non-member (over-fit, discarded). (ii) Large members ($13,22,23,72,76,81,95$) have no nontrivial binomial or simplex representation; small members are simplex in several $d$ at once ($10=\binom{5}{2}=\binom{5}{3}$, etc.), so "natural $d$" is ill-defined. (iii) Multiplicative-resonance count is not anomalous ($P=0.15$, dominated by trivial $\times1$). (iv) Complement-side: holes are no cleaner than members in any test. **Reduction to the generation DAG (the geometric-index frontier).** Why no static $f$ works is now explicit: $n$ is not a function of $(d,k)$ — it is the OUTPUT of the generation step at depth $k$. The documented step is one of three operation types: hockey-stick $S(\cdot,d_{\rm eval})$ with $d_{\rm eval}\in\{3,4\}$ (the two seed sectors — down's $S^3$, up's $\mathbb{CP}^2$), additive $a{+}b$, and inclusion–exclusion $a{+}b{-}c$, plus the one-off Euler keystone $n_{\rm top}=\chi(\mathbb{CP}^2)\chi(\mathbb{CP}^3)\chi(\mathbb{CP}^5)=72$. Concretely $\nu_1{=}S(3,3){=}10$, $\nu_2{=}S(3,4){=}15$, charm${=}S(4,3){=}20$, $\mu{=}S(4,4){=}35$ (hockey-stick into $d_{\rm eval}{=}3$ or $4$); $e{=}\nu_1{+}\mathrm{up}{=}13$, $\nu_3{=}\nu_1{+}\nu_2{-}\mathrm{up}{=}22$, $\tau{=}\nu_3{+}\mathrm{down}{=}23$ (additive/IE). So the residue is precisely **which operation and operand fire at each DAG node** — the binary channel $d_{\rm eval}\in\{3,4\}$ for the hockey-stick primaries plus the composite edges — exactly the run #4/#5/#8 operand-selection problem, which is the dynamical MC-4 question, not a static combinatorial cut. **Net of the whole $(n,d,k)$ battery (Batches 1–4 + esoteric): $d=k+c$ ($c\in\{1,2,3\}$) is the firm law (the $d$-half off $k$, ⭐/✅); the $n$-half is the generation-DAG firing rule, which no static function of $n,d,k$ (or its complement) reproduces — it reduces to the geometric question of which Hopf-chain channel ($d_{\rm eval}\in\{3,4\}$) the hockey-stick takes at each step, the open MC-4 selection.**

**MC-4 conservative-dynamics pass on the composite edges — exhaustive null (2026-06-14).** With the established masses $E=S(n,d)\,m_{\rm scale,d}$ ($m_{\rm scale}$: $d2{=}27.47$, $d3{=}4.702$, $d4{=}0.1451$, $d5{=}7.4\times10^{-13}$, $d6{=}d10{=}2.75\times10^{-5}$ MeV), the three composite edges $e{=}\nu_1{+}\mathrm{up}$, $\nu_3{=}\nu_1{+}\nu_2{-}\mathrm{up}$, $\tau{=}\nu_3{+}\mathrm{down}$ are **index-resonant exactly** ($10{+}3{=}13$, $10{+}15{-}3{=}22$, $22{+}1{=}23$) but conserve nothing else: energy detuning is gross (ratios $4.3$, ${\sim}10^7$, $0.003$ — cross-sector scales differ by up to $10^{13}$), $d$ is not conserved ($\Delta d=+3,+1,-2$), and a search over $Q=n+a\,d$ finds only $a=0$ (pure index) conserved on all three edges. Index conservation alone is the four-wave invariant of the conservative GP flow (run #8), and index-closure from the seeds reaches **all 95** indices — it over-generates. So the conservative nonlinear (four-wave) dynamics conserves the index and spreads over the whole additive closure; it cannot isolate the tower, confirming Part 4 §3.12's statement with explicit numbers. Together with the prior dynamical nulls — energy resonance (MC-4.4), BdG growth (Part 7 §1.2: non-members are not BdG-unstable), Hartree/kernel-eigenvector/conserved-charge (MC-4.4) — this exhausts the conservation-law and static-condition angles. **Net for T0.5 (whole 2026-06-14 program): the $d$-half is closed ($d=k+c$, $c\in\{1,2,3\}$); the $n$-half is selected by neither any static function of $(n,d,k)$ nor any conserved quantity of the conservative dynamics — the only surviving route is the genuinely dissipative time-domain nonlinear basin (which modes are attractors under the full EoM with radiation), not reducible to a conservation law or a combinatorial cut. T0.5 status unchanged (odd-level ⭐; even-level 🔵/🔶); the open core is now sharply identified as the dissipative basin of the composite edges.** *(Update 2026-06-22: the even-level **exclusion** of non-members is no longer 🔵 — it is exact (✅) via the strictly positive radiative width, STEP 118; the 🔶 here is the positive **selection** of which modes fire as the tower, which this null record correctly leaves open.)*

**Backward pass + dissipative-width pass — both null (2026-06-14, Fedge "run it backwards, then the honest next step").** *Backward (self-consistent decomposition):* requiring each index to be produced by one documented operation from strictly-lighter MEMBERS only — $13/14$ members so decompose, and **top is the unique irreducible** ($72=\chi\chi\chi$, not a combination of lighter members — confirms run #5) — but $72$ of $81$ non-member indices are also explained (the additive $a{+}b$ with down$=1$ in the pool is too liberal), so the backward direction over-generates exactly as the forward one does. No tightening. *Dissipative width (the honest next step):* over the full candidate lattice, the proxy radiative width $\Gamma\sim J(n_r,3/2)^2\,\Delta E^2$ ($\Delta E=(S(n,d)-S(n{-}2,d))m_{\rm scale,d}$, downward link $(n,d)\to(n{-}2,d)+q$) gives NO sharp threshold — $552$ of $556$ non-members fall inside the members' $\Gamma$-band, the members are not the within-sector width-minima, and the composite edges split ($e,\nu_3$ low-$\Gamma$; $\tau$ high-$\Gamma$). This reconfirms the 🔵 soft (mass-monotone) ordering of §15/STEP 64–65; the proxy does not select. **Status of the 2026-06-14 program (honest scope): the $n$-residue resisted every function CLASS tested — note the function space on three integers is infinite, so this is strong accumulated evidence, NOT a proof of impossibility.** Classes run and null, in both directions ($n\to d$ and $d\to n$) and complement-side: number-theoretic features ($\tau,\Omega,\omega$, lpf, digit sums base $2/3/4/7/10$, popcount, $r_2$, Zeckendorf, $p$-adic valuations $v_{2,3,5,7}$); modular (single, double, and seed-moduli $3,4,7,19,96$); products and symmetric polynomials ($nd,nk,dk,ndk,nd{+}nk{+}dk$ — min cardinality $7$, no banding); ratios; binomial-inversion and binomials of the pairs; figurate/simplex; per-ray polynomial $n(k)$ (multi-valued, no fit); structural-constant forms (Euler $\chi(d)$, couplings $g_{dd}$, seeds — only the documented hockey-stick forms match); backward self-consistent decomposition (over-generates; top the unique irreducible); perturbative dissipative width (no sharp $\Gamma$ cut). Separately, no CONSERVED quantity of the conservative four-wave dynamics selects (energy, index, $d$, $n{+}ad$ — only bare index is conserved, and it over-generates). **The route genuinely not closed is the full time-domain nonlinear-PDE basin (a faithful EoM solve, beyond a proxy). The durable new result is the $d$-half law $d=k+c$, $c\in\{1,2,3\}$. The $n$-residue search remains open — more function classes and the EoM derivation both continue.**
**Addendum — ring monomial $n$-assignment: $n$ increases with $\alpha$ within each site (❓, 2026-06-18; `files/idwt.py` STEP 89).** With Hypothesis H proved (STEP 74), the 12 on-ray deposits biject with the monomials of $R = \mathbb{R}[\omega_2,\omega_3]/(\omega_2^3,\omega_3^4)$. Two entries are forced: photon $\leftrightarrow 1$ and $\tau \leftrightarrow \omega_2^2\omega_3^3$ (both ✅). The remaining 10 assignments were computed by listing the particles at each ring height $m$ ordered by mode index $n$, and checking whether this order coincides with ordering by $\alpha$ (the number of $\omega_2$ factors). Result: $n$ is strictly increasing with $\alpha$ at every site where more than one particle appears — sites $j=3,4,5,6$ (all four checked: True, STEP 89). Explicitly:

| site $j$ | monomials (by $\alpha$) | particles (by $n$) |
|---|---|---|
| $j=3$ | $\omega_3, \omega_2$ | down($n=1$), strange($n=4$) |
| $j=4$ | $\omega_3^2, \omega_2\omega_3, \omega_2^2$ | up($n=3$), charm($n=20$), top($n=72$) |
| $j=5$ | $\omega_3^3, \omega_2\omega_3^2, \omega_2^2\omega_3$ | $\nu_1(n=10)$, $\nu_2(n=15)$, $\nu_3(n=22)$ |
| $j=6$ | $\omega_2\omega_3^3, \omega_2^2\omega_3^2$ | $e(n=13)$, $\mu(n=35)$ |

The pattern holds across all 10 non-forced deposits without exception. The candidate physical reading: $\omega_2$ is the generator from the $d=4$ ($\mathbb{CP}^2$) sector, whose evaluation channel $S(n,4) > S(n,3)$ for all $n$ (more $\mathbb{CP}^2$ directions = larger simplex count). Each additional $\omega_2$ application corresponds to a $d_{\rm eval}=4$ type insertion, which produces higher $n$ than a $d_{\rm eval}=3$ insertion. So "more $\mathbb{CP}^2$ content $\Rightarrow$ higher $n$" is consistent with the documented hockey-stick evaluation channels. Status: ❓ — the pattern holds for all non-forced entries; no derivation. This is the T0.5 question recasted as a single monotonicity rule: given H, the within-bidegree $n$-assignment question reduces to "why does $n$ increase with $\alpha$?"

**Sharpening — the hockey-stick sub-layer carries no selection; the residue is the composite edges only (2026-06-14, opus).** Batch 4 named the residue as "which operation and operand fire — the binary channel $d_{\rm eval}\in\{3,4\}$ for the hockey-stick primaries plus the composite edges." The hockey-stick part is not a residue. The four primaries are the **complete $2\times2$ grid** of the two seeds against the two seed sectors, $\{n_{\rm up}{=}3,\,n_s{=}4\}\times\{d_{\rm eval}{=}3,4\}$: $S(3,3){=}10{=}\nu_1$, $S(3,4){=}15{=}\nu_2$, $S(4,3){=}20{=}\mathrm{charm}$, $S(4,4){=}35{=}\mu$ — all four cells fire, none is omitted, so no choice is made among them (the seeds are $n_{\rm up}{=}\chi(\mathbb{CP}^2)$, $n_s{=}\chi(\mathbb{CP}^3)$; $d_{\rm eval}$ ranges over the two seed sectors $S^3,\mathbb{CP}^2$). The one true restriction in this layer is that evaluation is confined to the seed sectors $\{3,4\}$: hockey-sticking the seeds into $d=5,6$ would give $S(3,5){=}21,\,S(3,6){=}28,\,S(4,5){=}56,\,S(4,6){=}84$, none a tower index — but within that confinement the grid is exhaustive, so there is nothing to select. **Consequence for T0.5:** the index-generation selection lives entirely in (a) the composite/IE edges — which lighter indices combine ($e,\nu_3,\tau,\mathrm{strange}$) — and (b) the sector-home assignment $d_{\rm home}\neq d_{\rm eval}$ (the primaries are evaluated in $d{=}3,4$ but live in $d{=}5,4,6$), with the boson cascade and the $b$ beat already pinned (Euler keystone; $k_0=n_s^2$). The hockey-stick channel drops out of the open residue; the dissipative-basin question (above) inherits a strictly smaller target — the composite operands plus the home-sector map.

---

### §38. Meta's minuscule representation claims — verification (2026-06-14)

Meta proposed three mathematical routes connecting the IDWT tower to minuscule representation theory. All three were verified and found incorrect in their specific claims; the broad direction (minuscule posets may be relevant) is plausible but unestablished.

**(A) Coxeter number claim.** Meta: "$d$-values $\{2,3,4,5,6,10\}$ = Coxeter numbers of $A_1,A_2,A_3,D_4,D_5,E_6$ minus 1." Checked: the Coxeter numbers are $A_1=2, A_2=3, A_3=4, D_4=6, D_5=8, E_6=12$; subtracting 1 gives $\{1,2,3,5,7,11\}$, not $\{2,3,4,5,6,10\}$. **WRONG.** No variant of this mapping (ranks, half-Coxeter, dual Coxeter) produces $\{2,3,4,5,6,10\}$ from these six algebras.

**(B) Hockey-stick sum.** Meta: "$2926 = 1+3+6+\cdots+76$" (triangular numbers). Checked: $\sum_{k=1}^{76} T_k = \binom{78}{3} = 76{,}076$, not 2926. What 2926 actually is: $S(76,2) = \binom{77}{2} = 2926$, a single evaluation of the mass formula, not a cumulative sum. **WRONG.**

**(C) 15 irreducible $d$-complete poset shapes.** Meta: "15 irreducible $d$-complete poset shapes correspond exactly to the 15 IDWT modes." First reading checked minuscule poset *shapes*: the $A_n$ series alone gives infinitely many (one $k \times m$ rectangle per pair $k \le m$), so a literal shape count is infinite, and the only finite exceptional shape count is 3 ($E_6\,\omega_1$, $E_6\,\omega_6$, $E_7\,\omega_7$). That reading checked the wrong object — the "15" refers to *classes*, not shapes, and the count is genuine. The proper probe is part (G) below.

**(D) Exceptional minuscule dimensions.** Checked whether any IDWT $S(n,d)$ value equals an exceptional (non-$A_n$) minuscule representation dimension ($E_6$: 27; $E_7$: 56; $D_n$ vector: $2n$; $D_n$ spinor: $2^{n-1}$). Only one match found: $S(4,3)=20 = 2\cdot10$ (the $D_{10}$ vector representation). This is a small-number coincidence; 20 is not structurally motivated for $D_{10}$ from IDWT. Every other IDWT $S(n,d)$ misses all exceptional minuscule dimensions.

**(E) Tautological $A_n$ connection.** Every $S(n,d) = \binom{n+d-1}{d}$ equals the dimension of $\omega_d$ of $A_{n+d-2}$, which is a minuscule representation. This is true for ALL $(n,d)$ pairs, not just the tower — a tautology, not a selection.

**(F) APS $\eta$-invariant $n$-parity claim (Meta's route #1, 2026-06-14).** Meta: the Atiyah–Patodi–Singer $\eta$-invariant on $S^{2d-1}$ selects "the odd-$n$ series for $d$ odd and the even-$n$ series for $d$ even, which is why you see $(1,3)$ and $(4,3)$ but not $(2,3)$ or $(3,3)$." Checked against the tower $n$-values within each sector: $d=2$ carries $n\in\{0,76,81,95\}$ (parities even, even, odd, odd); $d=3$ carries $n\in\{1,4\}$ (odd, even); $d=4$ carries $\{3,20,72\}$ (odd, even, even); $d=5$ carries $\{10,15,22\}$ (even, odd, even). Every sector holds mixed $n$-parity, and Meta's own cited example $(4,3)$ has even $n$ in an odd sector. **WRONG**, and self-contradictory. The genuine IDWT parity result is on the harmonic level $N=n-1$, not $n$, and is per-sector: $\ell=0$ present iff $n$ odd (l-parity, the established $\bigstar$ result for odd levels, §15/STEP 64–66). Meta conflated $n$ with the level and asserted a uniform odd/even-$d$ split that the tower does not have.

**(G) $d$-complete poset classification — proper probe (2026-06-14).** Proctor's Dynkin-diagram classification (R. A. Proctor, *Dynkin diagram classification of $\lambda$-minuscule Bruhat lattices and of $d$-complete posets*, J. Algebraic Combin. 9 (1999) 61–94) sorts every irreducible $d$-complete poset into exactly 15 classes: (1) Shapes, (2) Shifted shapes, (3) Birds, (4) Insets, (5) Tailed insets, (6) Banners, (7) Nooks, (8) Swivels, (9) Tailed swivels, (10) Tagged swivels, (11) Swivel shifteds, (12) Pumps, (13) Tailed pumps, (14) Near bats, (15) Bat. So Meta's *count* is real; the cardinality coincidence $15 = 15$ is the only substance to the claim, and it does not survive contact with the structure.

Each class is an infinite family (Class 1 alone is every Young-diagram shape). The decisive test is which class each IDWT mode occupies. The mass $S(n,d)=\binom{n+d-1}{d}$ is the number of order ideals of the $d \times (n-1)$ rectangle poset (equivalently, the weights of $\omega_d$ of $A_{n+d-2}$ realized on the Grassmannian $\mathrm{Gr}(d,n+d-1)$). A rectangle is a **Class 1 "Shape"**. Verified for all 15 modes: each is the $d\times(n-1)$ rectangle and $\#\text{ideals} = \binom{(n-1)+d}{d} = S(n,d)$ exactly (e.g. $e$: $6\times12$, 18564; $\tau$: $10\times22$, 64512240; $\mathrm{up}$: $4\times2$, 15). All 15 modes collapse into the single class Shapes — they do not spread across the 15 classes, so there is no bijection and no structural content behind $15 = 15$. The classification describes the hook-length / $P$-partition generating function of *any* shape and selects no subset, so like (E) it is universal and non-selective. The route is now probed and closed.

**Genuine (previously known) connection.** $n_{\rm top} = \chi(\mathbb{CP}^2)\cdot\chi(\mathbb{CP}^3)\cdot\chi(\mathbb{CP}^5) = 3\cdot4\cdot6 = 72$ (already STEP 1). The Kähler sector Euler characteristics $\{2,3,4,6\}$ equal Coxeter numbers of $\{A_1,A_2,A_3,A_5\}$, which is just $\chi(\mathbb{CP}^k)=k+1 = h(A_k)$ — not new. The Kähler half-dimensions $d/2=\{1,2,3,5\}$ for the active even sectors $d\in\{2,4,6,10\}$ are consecutive Fibonacci numbers with a gap at $4$; that gap is $d=8$, the excluded even sector ($\chi$-gap, T15b), so the observation restates the known $d=8$ exclusion rather than adding to it.

**Verdict.** Meta's specific mathematical claims (Coxeter, hockey-stick, APS $n$-parity) are false. No new selective connection to exceptional Lie algebras was found. The $d$-complete poset / minuscule route is now probed and closed (G): the IDWT mass is the order-ideal count of a rectangular Class-1 Shape, every mode sits in the same one of Proctor's 15 classes, and the formula is universal over all $(n,d)$, so the framework carries zero selective power for the tower. The $15 = 15$ cardinality match is coincidence. Recorded to prevent re-running these checks.

---

### §44. Gegenbauer criticality at IDWT sectors recovers down-quark generation indices (2026-06-15, Opus)

**Setup.** The existing T5 analysis (`idwt.py` STEP 26; Part 9 §T5) uses the Gegenbauer coupling coefficient

$$b_{k_0}(d) = \frac{\sqrt{k_0(k_0 + d - 1)}}{2k_0 + d - 2}$$

with the resonance site fixed at $k_0 = n_s^2 = 16$ and asks: at which sector $d$ does $b = \tfrac{1}{2}$ (the extended/localised boundary)? The unique answer is $d = 10$, via the integer condition $4k_0 = (d-2)^2$, which terminates the sector chain.

**Observation.** Reversing the question — holding $d$ fixed and solving $4k_0 = (d-2)^2$ for $k_0$ — gives one critical $k_0$ per even sector:

| IDWT sector $d$ | critical $k_0 = \bigl(\tfrac{d-2}{2}\bigr)^2$ | IDWT identity |
|---|---|---|
| $d = 4$ ($\mathbb{CP}^2$, up-type quarks) | $k_0 = 1$ | $= n_{\text{down}}$ |
| $d = 6$ ($\mathbb{CP}^3$, electron) | $k_0 = 4$ | $= n_s = n_{\text{strange}}$ |
| $d = 10$ ($\mathbb{CP}^5$, tau/terminal) | $k_0 = 16$ | $= n_s^2 = $ bottom beat index |

The critical $k_0$ values at the three active even IDWT sectors are exactly the down-quark generation indices: $1, 4, 16 = n_s^0, n_s^1, n_s^2$. The same geometric sequence with ratio $n_s$ that appears as the down-quark mode indices in $d=3$ also emerges as the Gegenbauer critical endpoints of $d = 4, 6, 10$ respectively. Note that $d = 8$ (critical $k_0 = 9$) is not an active IDWT sector, so the correspondence is exact on the active set.

The connection to the bottom quark: the bottom uses $k_0 = 16$ as the beat index in $d = 3$ precisely because $16$ is the $d = 10$ Gegenbauer critical endpoint (the terminal sector's resonance site). The bottom sits at the top of the $d = 3$ generation tower and "inherits" the global Gegenbauer boundary.

**Status:** ⭐ structural identity. The coincidence $\bigl(\tfrac{d-2}{2}\bigr)^2 = n_{\text{down-gen}}$ at IDWT sectors is exact and arithmetically forced once $n_s = 4$ is given (since the down-quark indices are $n_s^0, n_s^1, n_s^2$ and the critical $k_0$ sequence is $1^2, 2^2, 4^2 = (n_s/2)^{2\cdot0}, (n_s/2)^{2\cdot 1}, (n_s/2)^{2\cdot 2}$ evaluated at $d = 4, 6, 10$). No dynamical claim.

**Implication for the top (null result).** The observation does not deliver a beat index for the top quark in $d = 4$. The $d = 4$-specific Gegenbauer critical $k_0 = 1$ gives a beat mass $\sqrt{S(1,4)\,S(2,4)}\times m_{\text{scale},4} \approx 0.32$ MeV — trivially small. Applying the global resonance $k_0 = 16$ to the $d = 4$ sector gives $\sqrt{S(16,4)\,S(17,4)}\times m_{\text{scale},4} \approx 629$ MeV — not the top. The index $k_0 = 72$ satisfies $4\times72 = 288 = (d-2)^2$ only at $d = 2 + \sqrt{288} \approx 18.97$, not an integer, so $72$ has no Gegenbauer criticality at any active sector. The $d = 4$ sector is supercritical ($b > \tfrac{1}{2}$) for all $k_0 > 1$ and approaches $\tfrac{1}{2}$ only asymptotically as $k_0 \to \infty$ — there is no large interior Gegenbauer endpoint in $d = 4$ analogous to $k_0 = 16$ at $d = 10$. The Gegenbauer route is therefore closed for the top; the beat/bifurcation mechanism for the top in $d = 4$, if it exists, must come from a different condition.

### §45. The up-quark sites as a second criticality branch $(d-1)(d-2)$ on the Kähler hosts, and the GTC exponent revision (2026-06-15, Opus)

**Setup.** §44 fixed the down-quark generation sites as the Gegenbauer (sphere) critical endpoints $(d-2)^2/4$ evaluated at the Kähler matter sectors $d\in\{4,6,10\}$: this gives $\{1,4,16\}$ exactly, the down-type indices, with the third-generation site $16$ inherited from the terminal sector $d=10$. The open question it left was the origin of the up-type sites $\{n_{\rm up}=3,\ n_{\rm charm}=20,\ n_{\rm top}=72\}$, and in particular a *dynamical* (criticality / bifurcation) address for the top's $72$ to replace the static count $72=N_c\,n_s\,N_f$ (demoted by the category objection in §43: a product of label cardinalities is not a frequency).

**Observation (site parallel).** Writing $\lambda=(d-2)/2$ (the Gegenbauer index of the down branch, with down site $=\lambda^2$), the up-type heavy-quark sites are the parallel quantity $2\lambda(2\lambda+1)=(d-1)(d-2)$ at the *same* host sectors:

| host $d$ | down site $(d-2)^2/4$ | up site $(d-1)(d-2)$ | IDWT |
|---|---|---|---|
| $d=4$ ($\mathbb{CP}^2$, gen $1$) | $1$ | $6$ | down $1$ / up $3=6/2$ (seed) |
| $d=6$ ($\mathbb{CP}^3$, gen $2$) | $4$ | $20$ | strange $4$ / **charm $20$** |
| $d=10$ ($\mathbb{CP}^5$, gen $3$) | $16$ | $72$ | bottom $16$ / **top $72$** |

Generations $2$ and $3$ match exactly: $20=(6{-}1)(6{-}2)$ and $72=(10{-}1)(10{-}2)$. The ratio up/down $=4(d-1)/(d-2)$ gives $5$ at $d=6$ and $4.5$ at $d=10$, reproducing $n_{\rm charm}/n_{\rm strange}=20/4=5$ and $n_{\rm top}/n_{\rm bottom\,site}=72/16=4.5$. Under this reading the heaviest quark of *each* type would inherit its site from the terminal sector $d=10$, with the generation index mapping to the host Kähler sector $\{$4, 6, 10$\}$.

**Correction — this does NOT source $72$; it re-spells it (Fedge's challenge, 06-15).** The "Observation" above is circular as a derivation of the site. $20$ and $72$ are the *accepted tier-2 inputs* ($n_{\rm charm}=20$, $n_{\rm top}=72=N_c\,n_s\,N_f$, STEP 1); the formula $(d-1)(d-2)$ was reverse-engineered to pass through $\{(6,20),(10,72)\}$. Evaluating it at $d=10$ therefore cannot *predict* $72$ — $72$ was used to *find* the formula. The reframe gives $72$ no origin it did not already have; it remains an input, exactly as §43 tier-2 had settled. The asymmetry with the down branch is the whole point: there the site $16$ is the critical endpoint of an *independently defined, derived* object — the Gegenbauer coupling coefficient $b_{k_0}(d)$ (STEP 26) genuinely equals $\tfrac12$ at $d=10$ when $k_0=16$, so $16$ falls out of a spectral condition. The up branch has **no such coefficient**: $(d-1)(d-2)$ is asserted by the fit, not obtained from a Kähler/Jacobi coupling coefficient. Until such a coefficient is written down and shown to cross its threshold at $(d-1)(d-2)$, the "parallel" is cosmetic and $(d-1)(d-2)|_{d=10}=72$ is an identity, not a derivation. Status of the site reframe: **❓** (downgraded from 🔶) — a pointer toward where a Kähler criticality might live, not a result. What survives is *conditional on the input $72$*: the GTC $k$-value result below constrains the top's correction *given* its site and excludes the stale $\{$0, 3, 10$\}$; it makes no claim about where $72$ comes from.

**RESOLUTION (2026-06-16, Fedge): the GTC was REMOVED entirely, not refined.** The $\{$0, 7, 16$\}$ revision investigated in this section was *not adopted*. Up-type masses are quoted bare; charm and top overshoot PDG as open residues (CLAUDE.md; Part 2 §11). Do not reintroduce $(1-\varepsilon)^k$ with any exponent ($\{$0, 3, 10$\}$ or $\{$0, 7, 16$\}$). The investigation below is retained as the dated record of why the correction was dropped — its own decisive-test entries (from "Decisive test — DONE", 2026-06-15) already found the correction underived, and the 06-16 removal supersedes the entire $k$-value question.

**Mass test (pre-removal investigation) — the GTC exponents were proposed as physical $k$-values (phase load per up-type quark), claimed to be fixed by the derived scale rather than fitted.** The proposed correction was $m=S(n,4)\,m_{\text{scale},4}\,(1-\varepsilon)^{k}$, $\varepsilon=1/(280\sqrt7)$, with $k$ the accumulated $l=2$ phase load (up $k=0$). The current assignment $k\in\{$0, 3, 10$\}=\{$0, $n_{\rm up}$, $n_{\nu_1}$$\}$ carries the "generation depth" reading whose top justification is stale. The corrected $k$-values are **$\{$0, 7, 16$\}$**, and these are *fixed by the derived $m_{\text{scale},4}$ and $\varepsilon$*, not chosen to match PDG:

For each candidate integer $k$, charm and top each imply the $m_{\text{scale},4}$ they would require to land on PDG 2024. Only at $(k_{\rm charm},k_{\rm top})=(7,16)$ does the implied scale equal the *independently derived* $m_{\text{scale},4}=0.145102$ MeV for both simultaneously — charm$\to0.145126$, top$\to0.145082$ (agreeing with the derived value to $0.02\%$). The current $(3,10)$ implies $0.144344$ (charm) and $0.143911$ (top): $\sim 1\%$ below the derived scale and mutually inconsistent by $0.3\%$. So $(7,16)$ is selected by self-consistency with the derived scale; no other integer pair (e.g. $(6,15)$, $(8,17)$) reproduces the derived scale from both quarks.

- top, $k=16$: $S(72,4)\,m_4(1-\varepsilon)^{16}=172{,}594$ MeV vs PDG $172{,}570\pm290$ → $+0.08\sigma$. (Current $k=10$: $173{,}999$ MeV, $+4.9\sigma$.)
- charm, $k=7$: $S(20,4)\,m_4(1-\varepsilon)^{7}=1272.8$ MeV vs PDG $1273.0\pm 4.6$ → $-0.05\sigma$. (Current $k=3$: $1279.7$ MeV, $+1.45\sigma$.)
- up, $k=0$: unchanged, $S(3,4)\,m_4=2.18$ MeV.

**$k$-value identities.** $k_{\rm charm}=7=n_s+n_{\rm up}$ (the argument of the universal $\sqrt{7}$); $k_{\rm top}=16=n_s^2=k_0$, the resonance site $=$ the down-gen-$3$ (bottom) site $=$ the $d=10$ Gegenbauer endpoint. Unified phase-load rule $k=g(g+2n_{\rm up})=(g+n_{\rm up})^2-n_{\rm up}^2$ in the generation index $g\in\{$0, 1, 2$\}$: a monic zero-constant quadratic $k=g^2+bg$ returns the same $b=6=2n_{\rm up}$ from charm ($g{=}1$) and top ($g{=}2$) independently. (At $n_s=4$ the rule's $g{=}2$ value $g(g+2n_{\rm up})=16$ coincides with $k_0=n_s^2$, since $4n_s=n_s^2$ at $n_s=4$ — the two readings of $k_{\rm top}$ agree.)

**Top as a resummed correction at the critical endpoint.** Because $\varepsilon=g_{\rm coeff}/(k_0\,n_\mu)$ with $g_{\rm coeff}=2/\sqrt{7}$, $k_0=16$, $n_\mu=S(n_s,4)=35$, the top's $k=k_0$ correction is $(1-\varepsilon)^{k_0}=(1-g_{\rm coeff}/(k_0 n_\mu))^{k_0}\approx\exp(-g_{\rm coeff}/n_\mu)$. The resummed form $\exp(-g_{\rm coeff}/n_\mu)=\exp(-2/(35\sqrt{7}))=0.978634$ has **no free exponent** (both $g_{\rm coeff}$ and $n_\mu$ are derived) and gives $172{,}597$ MeV ($+0.09\sigma$) — agreeing with $(1-\varepsilon)^{16}$ to $0.1$ MeV. This mirrors the $\tau$ at the same $d=10$ critical endpoint, where the divergent series resums to $1+1/1680$; the top, inheriting the $d=10$ endpoint as its site, takes the analogous all-orders form rather than a truncated power.

**Top is not a $d=4$ beat (control).** The bottom's mechanism (a beat $\sqrt{S(k_0,3)S(k_0{+}1,3)}$ at its site) does not transfer: $\sqrt{S(71,4)S(72,4)}\,m_4=171{,}597$ MeV ($-3.35\sigma$) and $\sqrt{S(72,4)S(73,4)}\,m_4=181{,}198$ MeV ($+30\sigma$). The $d=4$ rungs grow too fast for an adjacent-site geometric mean to land the top; the up-type mechanism is the rung with the multiplicative $(1-\varepsilon)^k$ shift, not a beat. (The bottom beat control reproduces $4181$ MeV, $-0.29\sigma$, as expected.)

**Status and what is open.**
- The site parallel is **🔶**, weaker than the down branch (§44 is ⭐, three-point exact). The up branch matches generations $2$–$3$ ($20, 72$) but gen $1$ needs the seed exception ($(d-1)(d-2)=6$ at $d=4$, vs $n_{\rm up}=3=6/2$; $n_{\rm up}=3$ has its own anchor $\chi(\mathbb{CP}^2)=N_c$). As a bare two-point fit $(d-1)(d-2)$ is not unique (a one-parameter family of quarter-integer quadratics passes through $(6,20),(10,72)$); its weight comes entirely from the parallel to the verified down branch on the same hosts and the shared terminal-sector inheritance.
- The $k$-value revision $\{$0, 7, 16$\}$ was proposed as **🔵** (NOT adopted — the GTC was removed 2026-06-16; see RESOLUTION above): the integers would be determined by self-consistency with the derived $m_{\text{scale},4}$ and $\varepsilon$ (not fitted to PDG), each has a closed identity ($n_s+n_{\rm up}$; $n_s^2=k_0$), and the unified rule $k=g(g+2n_{\rm up})$ is consistent across both heavy up quarks. What remains **🔶** is the *derivation* of the phase-load rule from the kernel's $l=2$ accumulation — why generation $g$ loads $g(g+2n_{\rm up})$ steps, and (cleaner) why the top resums to $\exp(-g_{\rm coeff}/n_\mu)$ at the $d=10$ endpoint. The current $\{$0, 3, 10$\}$ is excluded: it cannot reproduce the derived scale from charm and top simultaneously.
- **Candidate mechanism for the factor $\sim4$** (up site $/$ down site $=4(d-1)/(d-2)\to4$): the Kähler hosts $\mathbb{CP}^m$ carry only the class-1 $(k,k)$ harmonics — the holomorphic quotient suppresses odd degrees, so the effective angular step is doubled relative to the sphere $S^d$. A doubled angular index enters the centrifugal/criticality quantity squared, $\sim4\times$. This is a plausibility argument, not a derivation; the Jacobi/$\mathbb{CP}^m$ analogue of the Gegenbauer coupling coefficient $b_{k_0}(d)$ and its endpoint condition have not been written down.

**Not yet propagated.** This is a structural fork (it changes the top's origin story and the up-type GTC exponents, touching the standing CLAUDE.md rule $k\in\{0,3,10\}$, `idwt.py` STEP 4, and the charm input to the $D$-meson masses in STEP 7a). Recorded here only; `idwt.py`/Part edits await Fedge. Method is fully specified by the formulas above (sites $(d-2)^2/4$ and $(d-1)(d-2)$ at $d\in\{4,6,10\}$; masses $S(n,4)\,m_4(1-\varepsilon)^k$ with $k=g(g+2n_{\rm up})$) and reproduces with the STEP 1–4 constants.

**What the GTC correction is actually coming from — and why the $k$ is a stand-in (Fedge's challenge, 06-15).** Pressed on "with no $k$-value, where does the correction come from," the honest accounting: with $\varepsilon$ fixed by its factors, the *derived* content of $(1-\varepsilon)^k$ is just $\varepsilon$; the per-quark exponent is **chosen, not derived**, so the $(1-\varepsilon)^k$ bookkeeping is a fit — and that verdict includes the revised $\{0,7,16\}$ (better constrained, since it must be consistent with the derived $m_{\text{scale},4}$, but still a fit *parameter*, not a mechanism). The top's $\exp(-g_{\rm coeff}/n_\mu)$ is $k$-free only because $k=k_0$ cancels the $k_0$ in $\varepsilon$; the charm has no such cancellation.

But the correction is *modelling a real effect*. Bare $S(n,4)\,m_{\text{scale},4}$ overshoots PDG **systematically and growing with generation**: $u+0.8\%$, $c+0.9\%$, $t+2.2\%$ — not scatter; something pulls the heavier up-quarks down. The genuine mechanism candidate is exact: $(\xi\cdot\xi')^2=\tfrac1d[l{=}0]+\tfrac{d-1}{d}[l{=}2]$. The $l=0$ piece sets the sector mass *scale*; the $l=2$ piece is **traceless**, so it contributes nothing to the scale but shifts each mode by a mode-specific amount — the only place a generation-dependent correction can originate. Because the $l=2$ tensor is traceless, its first-order shift averaged over a degenerate level **vanishes**; the real shift is **second order**, $l=2$ connecting mode $n$ to $n\pm 2$: $\delta_n\sim|\langle n|l{=}2|n{\pm}2\rangle|^2/(\Delta E)$. This single fact reproduces everything $(1-\varepsilon)^k$ fakes with $k$: the shift is small (second order in the $l=2$ coupling), grows with $n$ (matrix elements grow), and can blow up / resum near a resonance (small denominator) — which is *why* the top, at the $d=10$-inherited resonance, resums. So $(1-\varepsilon)^k$ is a crude proxy for this second-order self-energy, and $k$ impersonates its growth.

**Honest $k$-free reformulation + decisive test (NOT yet done).** The correction for mode $(n,4)$ *is* its second-order $l=2$ self-energy — a definite, computable overlap on $\mathbb{CP}^2$ (the same Jacobi machinery the site question needs), with **no $k$**. Test: compute it directly and check whether it reproduces $-0.9\%/-2.2\%$ for charm/top. If yes → the GTC is derived, $k$ vanishes, and the up-type masses are earned. If no → the GTC has no origin and the honest move is to **strip it**, presenting the up-type masses bare with $+0.8/+0.9/+2.2\%$ errors. This $l=2$ self-energy is the real open problem behind both the GTC and (via the same $\mathbb{CP}^2$ spectral machinery) the Kähler-criticality site question; the two are one calculation. Status: **🔶/❓** — qualitative mechanism real (the $l=2$ anisotropy is mandatory), quantitative content uncomputed.

**Decisive test — DONE (2026-06-15, Opus). Mechanism confirmed real; quantitatively does NOT derive the correction. Verdict: mixed, no status upgrade.**

The second-order $l=2$ cross-sector self-energy was computed explicitly. Setup (Part 2 §11, §0.6): the up-type mode $(n,d{=}4)$ couples through the $l=2$ part of the cross-sector kernel $g_{34}(\xi_3\cdot\xi_4)^2$ to the $d=3$ seed (down, ground state); the $l=2$ tensor excites **both** legs ($d{=}4$ mode $n\!\to\!n'$ at shell $N\!\to\!N\pm2$, and $d{=}3$ seed $\to$ its $l=2$ excitation). All $d=3$-side and angular ($CG$) factors are $n$-independent and fold into the overall prefactor; the entire $n$-dependence is the $d=4$ radial element $|\langle N{\pm}2|r^2|N\rangle|^2$ (exact isotropic-HO ladder value $((n_r{+}1)(n_r{+}l{+}d/2))/w_4^2$, $w_4=\sqrt{\lambda_4}$) over the energy denominator, with mode index $\to$ shell $N=n$. The fractional **mass** shift follows by propagating the harmonic-energy shift through the prism Jacobian, $\delta m/m=(S'(n)/S(n))\,\delta N$, $\delta N=\delta E/2w_4$.

Findings (robust across the $N{=}n$ vs $N{=}n{-}1$ map and the representative $(n_r,l)$ choice):

1. **Sign correct.** $\delta m/m<0$ for all $n$ — the $l=2$ self-energy pulls the overshooting masses *down*, as required. (Both intermediate channels sit above the initial state because the $d{=}3$ excitation gap $2w_3$ exceeds the $d{=}4$ de-excitation $2w_4$, so both denominators are negative.)

2. **Grows with $n$, near-linearly.** The shape exponent is $p\approx 0.96$–$1.02$ in $\delta m/m\propto n^{p}$. The shape ratio $\mathrm{top/charm}=g(72)/g(20)\approx 3.4$–$3.7$.

3. **Convention-dependent existence of the effect.** The growing shape appears **only** with harmonic-energy denominators ($\mp 2w_4-2w_3$), which is the correct convention for perturbation theory in the harmonic-oscillator eigenbasis (the prism $S(n,d)\!\to\!$ mass is a relabelling of the *full* spectrum, not the PT denominator). Under the **prism-mass** denominators used in this appendix's only prior self-energy computation (§20, $n{=}3$), the shape *falls* with $n$ ($p\approx -3$) and the mechanism fails outright. The verdict therefore rests on the harmonic convention being the right one; this is defensible but is a genuine interpretive load-bearing choice and is flagged as such.

4. **Quantitative match: partial, and it favours the *old* $\{$0, 3, 10$\}$, not $\{$0, 7, 16$\}$.** The self-energy's near-linear shape ($p\approx 0.96$) matches the $\{$0, 3, 10$\}$ exponents ($\mathrm{top/charm}=10/3$, $p=0.94$) and **not** the consistency-selected $\{$0, 7, 16$\}$ ($\mathrm{top/charm}=16/7$, $p=0.65$). Taken with the framework's own $\varepsilon$ (i.e. normalising the magnitude to charm), it reproduces $k_{\rm eff}\approx\{$0.5, 3, 10.3$\}$ — essentially $\{$0, 3, 10$\}$ — which gives charm $1279.7$ ($+1.45\sigma$) and top $\sim\!173{,}900$ ($+4.6\sigma$) versus PDG 2024: the now-excluded predictions. The PDG-required corrections are $\{$5.6, 6.85, 15.94$\}\varepsilon$ (i.e. each quark's full overshoot $/\varepsilon$), whose $\mathrm{top/charm}$ ratio is $2.33$; the self-energy's $3.4$ is $\sim 45\%$ too steep.

5. **Affine reading (free $l=0$ base $+$ $l=2$ prefactor).** Allowing the two genuinely-free constants — $C$ (the open $l=2$ overlap prefactor, Part 2 §11.4) and $b$ (an $l=0$ scale offset) — and fitting $C\,g(n)+b$ to charm and top on PDG 2024 gives $C=1.51\times 10^{-3}$ (only $\sim 12\%$ above $\varepsilon=1.35\times 10^{-3}$ — right order) and $b=-0.42\%$, predicting up at $+0.08\sigma$. But this is a 2-parameter fit with effectively **one** out-of-sample point (up, $\pm 3\%$ error) and the fitted $b<0$ has the opposite sign to the documented $+0.77\%$ Level-1 base, so it has no clean home. The test is weak; it neither confirms nor excludes at the precision that separates $\{$0, 3, 10$\}$ from $\{$0, 7, 16$\}$.

**What this settles.** The $l=2$ self-energy is a **real, correctly-signed, monotonically-growing** mechanism whose best-fit prefactor lands within $\sim 10$–$40\%$ of the framework's $\varepsilon$ — so the Part 2 §11.4 open item is no longer "uncomputed": the mechanism exists and is order-of-magnitude right. But it does **not** derive the correction: with the derived $\varepsilon$ it reproduces the PDG-2024-**excluded** $\{$0, 3, 10$\}$, its shape is too steep for $\{$0, 7, 16$\}$, and the only reading that fits PDG is a weak 2-parameter fit. So neither integer set is earned — the mechanism's natural shape backs $\{$0, 3, 10$\}$ (which the data exclude) while $\{$0, 7, 16$\}$ (which the data prefer) has the wrong shape. **GTC stays 🔶**; do not upgrade to ✅; the up-type heavy-quark correction is a real-but-underived $\sim 1$–$2\%$ residual. This is the strip-or-keep evidence Fedge requested: the mechanism survives (do not strip on grounds of being a pure fudge), but it does not justify upgrading the status or claiming the masses are derived.

**Recorded in `idwt.py` STEP 86 (2026-06-15).** The self-energy computation (HO matrix elements, harmonic denominators, prism Jacobian) is now in STEP 86 of `files/idwt.py`. Key output: shape exponent $p=0.962$; applying $C=\varepsilon$ reduces generation-dependent scatter from $1.43\%$ to $0.20\%$; residuals quasi-uniform at $+0.48$–$+0.68\%$, consistent with the Level-1 $+0.77\%$ offset; top sits at $+3.67\sigma$ from the $\sim 12\%$ HO underestimate of the exact $\mathbb{CP}^2$ matrix element.

**Exact Fubini-Study computation — DONE (2026-06-15). Result: wrong basis; HO is the correct framework.** Computed via Monte Carlo on $S^5\times S^5$ ($N=500{,}000$ samples) and confirmed analytically. The total squared kernel is $\mathbb{E}[|V_2|^2]=\mathbb{E}[|(z\cdot z')|^2-1/3|^2]$ where $z, z'$ are uniform on the unit sphere in $\mathbb{C}^3$. Analytically: with $\langle|{<}z|z'{>}|^2\rangle=1/3$ and $\langle|{<}z|z'{>}|^4\rangle=2/(3\cdot 4)=1/6$, one gets $\mathbb{E}[V_2^2]=1/6-2/9+1/9=1/18\approx 0.0556$ (confirmed numerically). The per-matrix-element in the holomorphic degree-$n$ basis is $|m|^2=E_{\rm total}/(S(n,3)\cdot S(n+2,3))$, falling as $\sim n^{-5.7}$ — shape exponent $p\approx -5.69$, directly opposite to the HO's $p\approx +0.96$.

**Why the two computations give opposite shapes — and which one is right for IDWT.** The Fubini-Study (FS) matrix element uses the holomorphic polynomial basis on $\mathbb{CP}^2$, with complex degree-$n$ dimension $S(n,3)=\binom{n+2}{2}=(n+1)(n+2)/2$. The FS coupling falls with $n$ because the holomorphic modes become increasingly orthogonal on the compact manifold as $n$ grows (the Riemann–Lebesgue mechanism on a compact space). The HO basis uses real degree-$n$ polynomials in 4 variables, with dimension $S(n,4)=\binom{n+3}{3}$; the rising matrix elements come from the ladder-operator structure on the non-compact flat approximation. The IDWT mode counting formula is $S(n,4)$, not $S(n,3)$ — it matches the HO degeneracy, not the holomorphic $\mathbb{CP}^2$ degeneracy. Therefore **the HO is the correct mode basis for IDWT**, and the FS computation computes matrix elements in the wrong basis. The $\sim12\%$ prefactor gap cannot be closed by going to the FS holomorphic basis; that is a different object. What would be needed is the anharmonic correction to the HO on the actual sector potential — a harder calculation requiring the full $\mathbb{CP}^2$ sector Hamiltonian beyond the quadratic approximation. This is not pursued further here.

**Summary of §45 findings on the $l=2$ mechanism.** (1) Mechanism is real (correct sign, growing with $n$ in HO basis). (2) Shape $p\approx 0.96$ matches the PDG-excluded $\{$0, 3, 10$\}$, not the consistency-selected $\{$0, 7, 16$\}$. (3) The exact FS basis gives the wrong shape ($p\approx -5.7$) because it is a different mode basis than IDWT uses. (4) GTC stays 🔶; the prefactor gap ($C=1.119\varepsilon$ vs $\varepsilon$) cannot be closed via the FS route. (5) The decisive question — which exponent set to use, and whether to hold or publish — is a publication strategy call for Fedge (see the ASK FEDGE item in the project task list).

**First-order quadrupole of the polarized stretched state — the magnitude exclusion (2026-06-18).** The second-order route above rests on the first-order $l=2$ shift vanishing. The argument as stated is the trace argument: $T_{ij}$ is traceless, so its shift *averaged over a degenerate shell* is zero. That leaves a gap — a single mode need not be the shell average. The up-type modes are degree-$(n-1)$ monomials (Part 2 §1); a colour/spin-aligned quark would occupy the **polarized stretched state** $\chi_n\propto z_1^{\,n-1}e^{-\sqrt{\lambda_4}r^2/2}$ ($z_1=\xi_1+i\xi_2$), whose density $|z_1|^{2(n-1)}e^{-\sqrt{\lambda_4}r^2}$ is anisotropic and carries a **nonzero** first-order quadrupole. Its closed form is exact (verified by direct Gaussian moments and by Monte Carlo, `idwt.py` STEP 90):

$$Q^{(n)} = \frac{n-1}{4\sqrt{\lambda_4}}\,\mathrm{diag}(1,1,-1,-1),\qquad \|Q^{(n)}\|^2=\frac{(n-1)^2}{4\lambda_4}\quad\text{(⭐ identity).}$$

The first-order mean-field shift $\delta m/m=-(g_{44}/8\lambda_4)\big((n-1)/(n+1)\big)^2$ then evaluates to $-8.2\%$ (up), $-26.9\%$ (charm), $-31.1\%$ (top) — $10$–$30\times$ the required $-0.8$ to $-2.2\%$, and *saturating* with $n$ rather than growing. So the polarized stretched state is **excluded by both magnitude and shape**. This closes the gap the trace argument left open: not only does the shell-average first-order shift vanish, but the one single state that could have carried a first-order shift is ruled out independently. The physical up-type quark therefore cannot sit in a pure polarized stretched state; the diagonal first-order piece must (near-)cancel, leaving only the second-order $N\!\leftrightarrow\!N\!\pm\!2$ mixing of STEP 86 as the operative mechanism. Status: ⭐ (the moment) + null (the polarized-state route).

**Forward thread.** The same monomial-state Gaussian-moment machinery yields the *off-diagonal* elements $\langle N\!\pm\!2|T_{ij}|N\rangle$ in closed form. STEP 86 currently approximates these by the isotropic-HO ladder factors; replacing them with the exact monomial-basis values is the natural next computation toward a basis-correct second-order prefactor (it would not by itself resolve the $\{0,3,10\}$ vs $\{0,7,16\}$ shape question, but it removes the $\sim$12% HO-approximation gap noted above).

---

### §47. Holonomy as the home for CP PHASES (not the spectrum): the lepton CP phase IS a Hopf/Berry holonomy, and the quark/lepton CP asymmetry is a complex-vs-real sector statement (2026-06-16, Opus)

**Context.** Following the holonomy review (§46 addendum; §15 holonomy-selector null), the one place nontrivial (non-$Z_2$) holonomy was flagged as conceptually correct is *phases*, not the mass spectrum. This section records that the direction is already realised for the lepton sector and works out the quark analog, with Fedge's geometric correction.

**1. The lepton CP phase is already a Hopf/Berry holonomy (recognition, not new math).** Part 10 §1 derives $\delta_{CP}^{\rm lep}=\pi+(N_c-1)\theta_{13}=\pi+2\theta_{13}$ as the first Chern class of the determinant line bundle $\det U_{\rm PMNS}(\theta_{13})$, computed via the APS relation $c_1(\det L)={\rm sf}(D;{\rm path})$ with spectral-flow rate per $\theta_{13}$ equal to $c_1(\mathbb{CP}^n)=n+1$ (HRR: $\chi(\mathbb{CP}^n,\mathcal{O}(1))=n+1$). This *is* the Hopf $U(1)$/Berry holonomy proposed as the forward lead: the winding coefficient $\Delta c_1=c_1(\mathbb{CP}^5)-c_1(\mathbb{CP}^3)=6-4=2$ is the relative Berry phase between the two charged-lepton home sectors $\tau\in\mathbb{CP}^5$ ($d=10$) and $\mu\in\mathbb{CP}^3$ ($d=6$). Verified numerically: $180^\circ+2\times 8.551^\circ=197.10^\circ$, matching the document's $197.11^\circ$. The period-$\pi$ (half-angle) structure of the Dirac family (Part 10 §3) is the same universal $Z_2$ (spin double cover / "$l{=}0$ iff $n$ odd" parity, §15) identified as IDWT's operative holonomy: $\delta_{\rm lep}=\pi+2\theta_{13}$ wears the $Z_2$ baseline ($\pi$) and the Chern/Euler difference ($2$) on its face. So the "holonomy for phases" direction is **validated and unified with the proven result** — the lepton CP phase needs no re-derivation.

**2. The quark CP phase is the open analog (idwt.py line $\sim 832$; Part 5 §3f) — currently borrows the lepton value (🔶 erratum candidate).** At tree level IDWT sets $\rho=\eta=0$, $\delta^{\rm CKM}=0$, $|V_{ub}|=|V_{td}|=0$; nonzero values are flagged as requiring "the Hopf Chern–Simons loop integral (Part 5 §3f)." Part 5 line 90 then assigns $|V_{ub}|_{\rm lower}=A s_C^3$ *using the lepton phase* $\delta=\pi+2\theta_{13}=197.11^\circ$ (labelled 🔶). This borrowing is structurally unjustified (see pt 3) and should be flagged: the lepton $\delta$ is a two-Kähler-sector object and cannot be imported into the quark sector. **ASK FEDGE** before editing Part 5 — what replaces it is not yet pinned (pt 4).

**3. The structural result (Fedge's correction): the quark CP phase spans two ENTIRELY DIFFERENT geometries, and a clean two-sided Berry phase requires two complex sectors.** The lepton CP phase is clean because both charged-lepton sectors are Kähler/complex peers — $\mathbb{CP}^3$ and $\mathbb{CP}^5$ — each carrying a genuine holomorphic line bundle $\mathcal{O}(k)$, a well-defined $c_1$, and a Berry connection, so the relative spectral flow $\Delta c_1=2$ is a genuine relative holonomy between two like objects. The quark sectors are **not** a peer pair: up-type lives in $\mathbb{CP}^2$ ($d=4$, Kähler/complex, $c_1=3$, the $\mathrm{SU}(3)$/colour sector) while down-type lives in $S^3$ ($d=3$, a **real** sphere — $\chi=0$, isometry $\mathrm{SO}(4)=\mathrm{SU}(2)\times\mathrm{SU}(2)$, **no global complex structure, no holomorphic $\mathcal{O}(k)$, vector-like**; CLAUDE.md real-vs-Kähler split). A Berry/Hopf holonomy is native to complex geometry; the real sector carries no intrinsic holonomy of its own. Consequences: (i) the quark CP phase is **not** a relative holonomy between two complex sectors — it is one-sided, sourced only by the $\mathbb{CP}^2$ (up) connection relative to a real (zero-holonomy) partner, or generated through the cross-sector coupling that lends $S^3$ its chirality (CLAUDE.md: down-type chirality is inherited from the adjacent Kähler $d=4$, not intrinsic). (ii) This is the holonomy-native reason the quark and lepton CP structures differ so sharply (and why the measured $J_{\rm CKM}\sim 3\times 10^{-5}$ is tiny while leptonic CP is large): CP violation requires complex phases, and complex structure is native to the Kähler sectors only — the down-type real sector cannot *source* a CP phase. (iii) It explains why the naive transcription fails quantitatively: forcing $S^3$ to act as a $\mathbb{CP}^1$ peer ($c_1=2$, the Hopf base) gives $\Delta c_1=3-2=1$ and $\delta_q\sim 1\times\theta\approx 13^\circ$ (Cabibbo) or $0.2^\circ$ ($V_{ub}$), nowhere near the measured $\gamma\approx 65.5^\circ$ — because $S^3$ is *not* a $\mathbb{CP}^1$ peer; its complex content is only the derived lower-dimensional Hopf base, not the sector's own geometry.

**4. Status and open piece.** Solid (recognition): the lepton CP phase is a determinant-bundle holonomy with coefficient $c_1(\mathbb{CP}^5)-c_1(\mathbb{CP}^3)=2$ and $Z_2$ baseline $\pi$ (Part 10, ✅). Novel structural claim (🔶, defensible from the real-vs-Kähler split): a CP phase is a relative Berry holonomy and a *clean two-sided* phase requires both participating sectors to be Kähler — leptons qualify (two complex sectors), quarks do not (up complex, down real). Open: the *magnitude* of the quark phase is not pinned — it needs the actual $d{=}4\!\leftrightarrow\!d{=}3$ ($\mathbb{CP}^2\leftrightarrow S^3$) cross-sector kernel-overlap phase (the analog of the lepton $\theta_{13}$ log-ratio), computed honouring that one side is real. Naive transcription is excluded (pt $3$ iii).

**5. Explicit mode-function analysis — order-of-magnitude DERIVED + localized to $V_{ub}$ (2026-06-24, Opus; `files/idwt.py` STEP 6).** Writing the explicit CP² holomorphic $\mathcal{O}(k)$ up-sections (the documented blocker) and the real $S^3$ down-harmonics in shared Hopf coordinates closes the structure and pins the magnitude to one residual. (i) **Charge selection (derived; was asserted in pt 2/3):** the kernel $(\xi_3\cdot\xi_4)^2$ carries Hopf charge $\in\{-2,0,+2\}$, so a chiral up-mode of charge $+k$ overlaps a real $S^3$ down-mode only if $k\le 2$. Only $u$ ($k=2$) couples; $c,t$ ($k=19,71$) give exactly zero. The quark CP phase therefore sits on $V_{ub}$ alone. (ii) **Real-beat theorem:** the bottom-quark beat ($n=16$), sustained by the framework's *real* couplings (Jacobi $\sqrt{b_{16}b_{17}}$ + the real quartic density term, Part 2 §8), phase-locks to $\chi\in\{0,\pi\}$ $\Rightarrow J=0$; a nonzero $J$ requires a genuinely complex Fubini–Study holonomy $\phi_{\rm link}$ on the $16\leftrightarrow17$ link, with $\chi=\pi-\phi_{\rm link}$ and $\sin\phi_{ub}=\sin\phi_{\rm link}$. (iii) **Magnitude:** the FS holonomy $=(\text{transported degree }k_u{=}2)\times 2\pi\times(\text{enclosed FS-area fraction})$; the clean node-cell reading $f=1/k_0$ gives $\phi_{\rm link}=k_u\cdot 2\pi/k_0=45^\circ$, so $J=|V_{us}V_{cb}V_{ub}V_{cs}|\sin\phi_{\rm link}=2.36\times10^{-5}$ — the same **order** as PDG $3.08\times10^{-5}$ (~23% low), the smallness carried entirely by the independently-derived $|V|$ product ($\phi_{\rm link}$ an $O(1)$ Kähler angle). **Open (🔶):** the exact enclosed area fraction. The PDG-matching $\phi_{\rm link}=67.5^\circ$ requires degree $3$ ($c_1(\mathbb{CP}^2)=N_c$), *not* the transported $\mathcal{O}(2)$ section's $k_u=2$ — a substitution rejected by the first-principles holonomy (it would transport a degree-3 object that is not a section here). So $J$ is derived to order of magnitude and localized; the exact value awaits the forced area fraction.


### §46. $n_{\rm top} = 72$ origin search — CLOSED (2026-06-18, Fedge)

Full log not reproduced. All spectral tests (Gegenbauer criticality on $\mathbb{CP}^2$, Kähler
invariants, sector-mode fixed-point equations, consecutive-pair structures) returned
first solutions smaller than $72$ or landed on a different $(n,d)$. No first-principles
criticality condition singling out $n=72$ by minimality was found. The identity
$72 = \chi(\mathbb{CP}^2)\times\chi(\mathbb{CP}^3)\times\chi(\mathbb{CP}^5) = N_c\cdot n_s\cdot N_f$ is recorded as a value identity (T15b).
$n_{\rm top} = 72$ is accepted as a seed input — on the same footing as $n_d=1$ and $n_u=3$ —
and no further derivation of its resonance origin is in progress.

### §49. Bottom beat $k_0 = n_s^2 = 16$ — CLOSED (2026-06-18)

Full log not reproduced. $k_0 = n_s^2 = 16$ is the Gegenbauer $b=1/2$ critical endpoint on $S^3$
(T5, Part 1 §4b; `files/idwt.py` STEP 1). The quartic-bifurcation bottom-beat formula
$m_b = \sqrt{S(16,3)\cdot S(17,3)}\cdot m_{\rm scale,3} = \sqrt{816\cdot 969}\cdot m_{\rm scale,3}$ is derived (Part 2 §11,
`files/idwt.py` STEP 44). The GTC correction scale $\varepsilon = 1/(280\sqrt{7})$ is derived from the
$\ell=2$ self-energy loop (idwt.py STEP 75–76), but no integer exponent set $k$ is forced
(the per-quark exponent was a fit, removed 2026-06-16). Record: idwt.py STEP 44 and
CLAUDE.md "GTC removed" note.
